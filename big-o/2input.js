const boxes = ["a", "b", "c", "d", "e"];
const boxes = ["f", "g", "h", "i", "j"];

function DifferentInput(boxes, boxes2) {
	for (let i = 0; i < array.length; i++) {
		console.log(boxes);
	}
	for (let j = 0; j < array.length; j++) {
		console.log(boxes2);
	}
}

DifferentInput(boxes, boxes2);

// O(a+b)
